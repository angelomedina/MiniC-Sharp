class AllProds

{ 

    int  ocuta(){
      
        if(len(a) > 5){
           read(ols.ee);
            xyz = len(w);
            as[-len(d)] = ss;
            return ord(d);
       }
       return len(s);
     
   }
    

}
